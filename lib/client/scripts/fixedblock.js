/*jQuery(function($) {
  function fixDiv() {
    var $cache = $('#fixedblock');
    var offset = $('#fixed_indicator').offset();
    if ($(window).scrollTop() > offset.top+60)
      $cache.css({
        'position': 'fixed',
        'top': '10px'
      });
    else
      $cache.css({
        'position': 'relative',
        'top': 'auto'
      });
  }
  $(window).scroll(fixDiv);
  fixDiv();
});*/