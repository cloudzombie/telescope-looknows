/*Posts.views.add("tranding", function (terms) {
  return {
  	find: {
      sticky: true
    },
    options: {sort: {sticky: -1, postedAt: -1}}
  };
});

Posts.controllers.tranding = Posts.controllers.list.extend({
  view: 'tranding'
});

Meteor.startup(function () {
  Router.route('/tranding/:limit?', {
    name: 'posts_tranding',
    controller: Posts.controllers.tranding
  });
});*/